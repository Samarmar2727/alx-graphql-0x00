# Character Queries

This task includes GraphQL queries to fetch character details by ID (1 to 4) and their JSON responses.

Each query fetches:

- id
- name
- status
- species
- type
- gender
- image
